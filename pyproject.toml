[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "monitui"
version = "0.1.0"
description = "TUI for monitor configuration"
dependencies = [
    "textual>=0.80.0",
]